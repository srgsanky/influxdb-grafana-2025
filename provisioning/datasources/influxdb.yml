apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8181
    isDefault: true
    editable: true
    database: ${INFLUXDB_DATABASE}
    jsonData:
      version: InfluxQL
      dbName: ${INFLUXDB_DATABASE}
      httpMode: POST